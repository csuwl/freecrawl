apiVersion: v1
kind: Service
metadata:
  name: {{ include "freecrawl.fullname" . }}-redis
spec:
  type: {{ .Values.service.redis.type }}
  selector:
    app: {{ include "freecrawl.name" . }}-redis
  ports:
    - protocol: TCP
      port: {{ .Values.service.redis.port }}
      targetPort: {{ .Values.service.redis.port }}
