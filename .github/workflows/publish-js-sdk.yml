name: Publish JS SDK

on:
  push:
    branches:
      - main
    paths:
      - apps/js-sdk/freecrawl/package.json

env:
  TEST_API_KEY: ${{ secrets.TEST_API_KEY }}

jobs:
  publish:
    name: Publish
    runs-on: blacksmith-2vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v6
      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
          cache-dependency-path: './apps/js-sdk/freecrawl/pnpm-lock.yaml'
      - name: Authenticate
        run: echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc
      - name: Publish
        run: |
          pnpm install
          pnpm run build
          pnpm publish --access public --no-git-checks
          sed -i 's/"name": "@mendable\/freecrawl-js"/"name": "@mendable\/freecrawl"/g' package.json
          pnpm publish --access public --no-git-checks
          sed -i 's/"name": "@mendable\/freecrawl"/"name": "freecrawl"/g' package.json
          pnpm publish --access public --no-git-checks
        working-directory: ./apps/js-sdk/freecrawl
